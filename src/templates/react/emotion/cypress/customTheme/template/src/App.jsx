import StyledDiv from './styles/ui/StyledDiv'
import StyledTitle from './styles/ui/StyledTitle'

function App() {
  return (
    <StyledDiv>
      <StyledTitle>Vite + React + Emotion + Cypress</StyledTitle>
    </StyledDiv>
  )
}

export default App
